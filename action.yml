name: 'Container Action Template'
description: 'Build Mozilla Firefox container'
author: 'tescales'
inputs: 
  myInput:
    description: 'Input to use'
    default: 'world'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.myInput }}